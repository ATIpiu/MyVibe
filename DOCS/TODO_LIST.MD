自动补全， 已经完成
包含1，文件名的补全，默认建立文件夹下的所有文件的索引除了.开头的文件。函数名变量名和类名也需要，如果使用@则选择进行文件名补全，如果用#则是变量名补全。
如果输入斜杠则有命令补全。命令补全的时候会显示简单的命令介绍。

不同模型调用分工 等待完成
便宜的模型用来做简单的代码编写
贵的模型用来做计划，以及思考如何出题。
如果包含图片，则调用VLM。

计划模式， 已经完成了

子Agent 待完成

自定义skill 接口

mcp相关接口 扩展

workload服务 模拟软件工程的工作节点的工作流，设置最大

cache缓存命中机制研究 已经完成

后台任务调出以及放入后台任务，选择是否展开详情(比如工具调用结果)。添加后台任务管理功能，可以查看当前存在哪些后台任务。

ctrl+c 中断当前聊天或者什么其他的东西 已经完成

调用pycompile工具完成验证，不至于编译的时候就出错 等待完成

目前的功能全部是基于python写的，包括记忆和上面的compile，考虑向其他语言扩展，

